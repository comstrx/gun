{
    "name": "workspace",
    "type": "module",
    "private": true,

    "scripts": {
        "test": "pnpm -r test",
        "build": "pnpm -r build",
        "typecheck": "pnpm -r typecheck"
    }
}
