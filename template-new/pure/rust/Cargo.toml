[package]
name = "demo"
version = "0.1.0"
edition = "2021"

[dependencies]

[profile.release]
codegen-units = 1
opt-level = 3
lto = "fat"
panic = "abort"
strip = "symbols"
incremental = false
overflow-checks = false
