1) process

مهم جدًا:

pid
ppid
is_pid
kill_pid
wait_pid
proc_alive
proc_name
2) temp

أنت عندك tmp_dir لكن ينقص:

new_tmp_file
new_tmp_dir
cleanup_tmp
with_tmp_dir
3) fs safety

مهم جدًا للإنتاج:

ensure_dir
ensure_file
safe_remove
safe_move
safe_copy
exists_or_die

5) path normalize

لو تريد قوة حقيقية:

abs_path
real_path
clean_path
resolve_path
6) command wrappers

أنت قريب جدًا هنا، لكن ينقص:

capture
capture_ok
capture_err
capture_status

8) trap / cleanup

مهمة جدًا جدًا:

on_exit
on_err
on_int
cleanup_push
cleanup_run

10) text/file search helpers

خفيفة لكن مفيدة:

has_line
grep_file
replace_file
contains_file
